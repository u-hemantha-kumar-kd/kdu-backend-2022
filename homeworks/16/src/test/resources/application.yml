spring:
  datasource:
    url: jdbc:h2:mem:dvdrental
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 1
    jpa:
      hibernate:
        ddl-auto: create-drop
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect